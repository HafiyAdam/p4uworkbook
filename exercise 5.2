# Exercise 2: Write another program that prompts for a list of numbers as above and at the end prints out both the maximum and minimum of the numbers 
# instead of the average.


total = 0
count = 0
maxnum = None
minnum = None 
while True:
    try: 
        number = input('Enter a number:')
        if number == 'done': 
            break 
        else:
            number = int(number)
            if maxnum is None or number > maxnum:
                maxnum = number 
            if minnum is None or number < minnum:
                minnum = number 
        count += 1 # or count = count + 1
        total += float(number) # or total = total + float(number) 
    except: 
        print('Please enter a number or done when finished')

average = total/count

print ("Total:",total,"Count:",count,"Average:",average,'Smallest:',minnum,'Largest:',maxnum)
